<form method="post" action="{{ path('app_paiements_delete', {'id': paiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>

